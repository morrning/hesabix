<div class="container mb-5">
    <div class="row">
        <div class="col-sm-12 col-md-12">
            {{ form_start(form) }}
            <h3 class="text-primary">اطلاعات کسب و کار</h3>
            <div class="row">
                <div class="col">
                    {{ form_row(form.name,{'label':'نام کسب و کار'}) }}
                    {{ form_row(form.field) }}
                </div>
                <div class="col">
                    {{ form_row(form.legalName,{'label':'نام قانونی کسب و کار'}) }}
                    {{ form_row(form.type) }}
                </div>
            </div>
            <h3 class="text-primary">اطلاعات اقتصادی</h3>
            <div class="row">
                <div class="col">
                    {{ form_row(form.shenasemeli) }}
                </div>
                <div class="col">
                    {{ form_row(form.codeeghtesadi) }}
                </div>
                <div class="col">
                    {{ form_row(form.shomaresabt) }}
                </div>
            </div>
            <h3 class="text-primary">اطلاعات تماس</h3>
            <div class="row">
                <div class="col">
                    {{ form_row(form.keshvar) }}
                    {{ form_row(form.codeposti) }}
                </div>
                <div class="col">
                    {{ form_row(form.ostan) }}
                    {{ form_row(form.tel) }}
                </div>
                <div class="col">
                    {{ form_row(form.shahr) }}
                    {{ form_row(form.fax) }}
                </div>
            </div>
            <div class="row">
                <div class="col">
                    {{ form_row(form.address) }}
                </div>
            </div>
            <div class="row">
                <div class="col">
                    {{ form_row(form.website) }}
                </div>
                <div class="col">
                    {{ form_row(form.email) }}
                </div>
            </div>
            <h3 class="text-primary">اطلاعات مالی</h3>
            <div class="row">
                <div class="col">
                    {{ form_row(form.arzMain) }}
                </div>
                <div class="col">
                    {{ form_row(form.maliyatafzode) }}
                </div>
            </div>
            <div class="row">
                <div class="col">
                    {{ form_row(form.salemaliLabel) }}
                </div>
            </div>
            {{ form_widget(form.submit) }}
            <button class="btn btn-primary" onclick="loadComponent(Routing.generate('app_business_list'))">بازگشت</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
